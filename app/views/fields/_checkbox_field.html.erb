<%= label :submission, field.question_title, class: "field_label", id: field.id %>

<% field.field_options.each do |option| %>
	<br>
    <%= check_box_tag(option[0], option[1] ) %>
    <%= label_tag(option[0], option[0]) %>
<% end %>