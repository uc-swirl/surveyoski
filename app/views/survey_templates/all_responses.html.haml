%table
  = link_to "Download CSV", download_data_path(params[:id], "submissions")
  %tbody
    %tr
    - @questions.each do |title|
      %td= title
    - @submissions.each do |submission|
      %tr
        - submission.each do |response|
          %td= response